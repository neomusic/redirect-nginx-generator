{
  "name": "redirect-nginx-generator",
  "version": "1.0.1",
  "scripts": {
    "start": "npm run build && node index.js",
    "build": "rm -rf dist && babel src -d dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "nginx",
    "redirect",
    "tool",
    "WebOps",
    "generator"
  ],
  "bin": {
    "redirect-nginx-generator": "./index.js"
  },
  "author": "Giuseppe Aremare <g.aremare@palmabit.com> (https://github.com/neomusic)",
  "description": "Easily create nginx redirects from a csv file",
  "dependencies": {
    "colors": "^1.3.3",
    "commander": "^2.19.0",
    "csvtojson": "^2.0.8"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.3.1"
  }
}
